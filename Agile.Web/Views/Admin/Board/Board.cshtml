@using Agile.Entities;

@using (var bc = Html.TypeContext<BoardEntity>())
{
    @Html.ValueLine(bc, b => b.Name)
    @Html.ValueLine(bc, b => b.State)
    @Html.EntityLine(bc, b => b.Project)
    @Html.EntityList(bc, b => b.Lists)
}
