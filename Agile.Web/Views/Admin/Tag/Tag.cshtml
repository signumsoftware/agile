@using Agile.Entities;

@using (var tc = Html.TypeContext<TagEntity>())
{
    @Html.EntityLine(tc, t => t.Project)
    @Html.ValueLine(tc, t => t.Name)
    @Html.ValueLine(tc, t => t.Color)
}
