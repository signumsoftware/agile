@using Agile.Entities;

@using (var nc = Html.TypeContext<NotificationEntity>())
{
    @Html.ValueLine(nc, n => n.CreationDate)
    @Html.EntityLine(nc, n => n.User)
    @Html.EntityLine(nc, n => n.Card)
    @Html.ValueLine(nc, n => n.Type)
    @Html.ValueLine(nc, n => n.Message)
}
